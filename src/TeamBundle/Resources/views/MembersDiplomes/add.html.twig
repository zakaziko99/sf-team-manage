{% extends 'base.html.twig' %}

{% block title %}TeamManager: Add new diplome{% endblock %}

{% block body %}
    <h1>Add New Diplome</h1>
    <p>Please, fill the fields below:</p>

    <div class="panel panel-default">
        <div class="panel-body">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-success">Add Diplome</button>
            {{ form_end(form) }}
        </div>
    </div>

    <div class="row text-right">
        <a href="{{ path('diplomes_list') }}">Back to the diplomes list</a>
    </div>
{% endblock %}
